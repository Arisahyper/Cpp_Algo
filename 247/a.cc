#include <bits/stdc++.h>
using namespace std;

#define endl "\n"

void solve() {
    string s;
    cin >> s;

    cout << "0" + s.substr(0,3) << endl;
}

int main() {
    cin.tie(nullptr), ios::sync_with_stdio(false);

    solve();
    return 0;
}
